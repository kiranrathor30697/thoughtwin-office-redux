import { RegisterActionCreater } from "../action_creater/ActionCreater";

   export const rootReducer = (initialState,action) => {
      let newState = initialState;
      switch(action.type){
         case RegisterActionCreater:
         break;
      }
    }

  
